<!DOCTYPE html>
<html>
  {% include common/head %}
  <style>

    .container {
      max-width: calc(100% - 20px);
      margin: 0 auto;
    }

    #vis {
      border: 1px solid black;
      width: 100%;
      height: 900px;
      clear: both;
      margin-bottom: 10px;
    }

    .tooltip {
      position: absolute;
      top: 100px;
      left: 100px;
      -moz-border-radius:5px;
      border-radius: 5px;
      border: 2px solid #000;
      background: #fff;
      opacity: .9;
      color: black;
      padding: 10px;
      width: 300px;
      font-size: 12px;
      z-index: 10;
    }

    .tooltip .title {
      font-size: 13px;
    }

    .tooltip .name {
      font-weight:bold;
    }

  </style>
  <body class="mdl-color--grey-100 mdl-color-text--grey-700 mdl-base lead-summary">
    <div class="mdl-layout__container">
      <div class="mdl-layout mdl-layout--fixed-header mdl-layout--fixed-tabs">
        <main class="mdl-layout__content">
          <div class="container">
            <h4>Total Number of Samples collected per School <br />
                <small>
                  * 2000-2010 Sample data shown in red <br />
                  * 2016 Sample data show in green <br />
                </small>
            </h4>
            <div>
              <a href="#" id="all" class="button active">All Schools</a>
              <a href="#" id="split" class="button">Split by Sampling Year</a>
            </div>
            <div id="vis"></div>
          </div>
        </main>
      </div>
    </div>

    {% include common/scripts %}
  </body>
</html>
<script type="text/javascript">
  window.data = {
    lead: {
      2010: {
        samples: {{lead-samples-2010 | json}},
        summary: {{lead-summary-2010 | json}}
      },
      2016: {
        samples: {{lead-samples-2016 | json }},
        summary: {{lead-summary-2016 | json}}
      }
    }
  }

</script>
