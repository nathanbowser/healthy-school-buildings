<!DOCTYPE html>
<html>
  {% include common/head %}
  <style>

    .container {
      max-width: calc(100% - 20px);
      margin: 0 auto;
    }

    #vis {
      border: 1px solid black;
      width: 900px;
      height: 900px;
      clear: both;
      margin-bottom: 10px;
    }

    #vis svg {
      width: 100%;
      height: 100%;
    }

    .tooltip {
      position: absolute;
      top: 100px;
      left: 100px;
      -moz-border-radius:5px;
      border-radius: 5px;
      border: 2px solid #000;
      background: #fff;
      opacity: .9;
      color: black;
      padding: 10px;
      width: 300px;
      font-size: 12px;
      z-index: 10;
    }

    .tooltip .title {
      font-size: 13px;
    }

    .tooltip .name {
      font-weight:bold;
    }


    .label {
      font: 11px "Helvetica Neue", Helvetica, Arial, sans-serif;
      text-anchor: middle;
      text-shadow: 0 1px 0 #fff, 1px 0 0 #fff, -1px 0 0 #fff, 0 -1px 0 #fff;
    }

    .node {
      cursor: pointer;
    }

    .node:not(.node--root):hover {
      stroke: #000;
      stroke-width: 1.5px;
    }

    .node {
      stroke: #ddd;
      fill: transparent;
    }

    .node--leaf {
      fill: #beccae;
    }

    .node--root {
      stroke-width: 0;
    }


    .label,
    .node--leaf {
      pointer-events: none;
    }

  </style>
  <body class="mdl-color--grey-100 mdl-color-text--grey-700 mdl-base lead-by-ppb">
    <div class="mdl-layout__container">
      <div class="mdl-layout mdl-layout--fixed-header mdl-layout--fixed-tabs">
        <main class="mdl-layout__content">
          <div class="container">
            <h4>Show samples of collected data based on ppb in {{year}}</h4>
            <div>
              <div id="thresholds-selector">
                <label for="thresholds">Filter by ppb</label><select id="thresholds"></select>
              </div>
            </div>
            <div id="vis"><svg></svg></div>
          </div>
        </main>
      </div>
    </div>

    {% include common/scripts %}
  </body>
</html>
<script type="text/javascript">

  {% if year == 2010 %}
    window.data = { lead: {{lead-samples-2010 | json}} }
  {% else %}
    window.data = { lead: {{lead-samples-2016 | json}} }
  {% endif %}

</script>
